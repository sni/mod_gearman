#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_clean:
	rm -fr .pc debian/patches
	dh_clean

override_dh_auto_configure:
	aclocal
	autoheader
	automake -a
	autoreconf --install --verbose
	dh_auto_configure
